replicaCount: 1

image:
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: payment-server-dev.akj.ru
      paths:
        - path: /
          pathType: ImplementationSpecific

resources: {}

envs:
  TEST_ENV: some-value